{
  "name": "my-next-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "npm run build && npm run export && gh-pages -d out",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "next": "^13.0.0", // Use the appropriate version
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
